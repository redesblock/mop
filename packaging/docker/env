# Copy this file to .env, then update it with your own settings

### MOP

## HTTP API listen address (default :1683)
# MOP_API_ADDR=:1683
## chain block time (default 15)
# MOP_BLOCK_TIME=15
## initial nodes to connect to (default [/dnsaddr/testnet.bnbcluster.org])
# MOP_BOOTNODE=[/dnsaddr/testnet.bnbcluster.org]
## cause the node to always accept incoming connections
# MOP_BOOTNODE_MODE=false
## enable clef signer
#MOP_CLEF_SIGNER_ENABLE=false
## clef signer endpoint
# MOP_CLEF_SIGNER_ENDPOINT=
## config file (default is /home/<user>/.mop.yaml)
# MOP_CONFIG=/home/mop/.mop.yaml
## origins with CORS headers enabled
# MOP_CORS_ALLOWED_ORIGINS=[]
## data directory (default /home/<user>/.mop)
# MOP_DATA_DIR=/home/mop/.mop
## cache capacity in chunks, multiply by 4096 to get approximate capacity in bytes
# MOP_CACHE_CAPACITY=1000000
## number of open files allowed by database
# MOP_DB_OPEN_FILES_LIMIT=200
## size of block cache of the database in bytes
# MOP_DB_BLOCK_CACHE_CAPACITY=33554432
## size of the database write buffer in bytes
# MOP_DB_WRITE_BUFFER_SIZE=33554432
## disables db compactions triggered by seeks
# MOP_DB_DISABLE_SEEKS_COMPACTION=false
## debug HTTP API listen address (default :1685)
# MOP_DEBUG_API_ADDR=:1685
## enable debug HTTP API
# MOP_DEBUG_API_ENABLE=false
## enable global pinning
## cause the node to start in full mode
# MOP_FULL_NODE=false
## NAT exposed address
# MOP_NAT_ADDR=
## ID of the Cluster network (default 1)
# MOP_NETWORK_ID=1
## P2P listen address (default :1684)
# MOP_P2P_ADDR=:1684
## enable P2P QUIC protocol
# MOP_P2P_QUIC_ENABLE=false
## enable P2P WebSocket transport
# MOP_P2P_WS_ENABLE=false
## password for decrypting keys
# MOP_PASSWORD=
## path to a file that contains password for decrypting keys
# MOP_PASSWORD_FILE=
## percentage below the peers payment threshold when we initiate settlement (default 25)
# MOP_PAYMENT_EARLY_PERCENT=50
## threshold in BZZ where you expect to get paid from your peers (default 100000000)
# MOP_PAYMENT_THRESHOLD=100000000
## excess debt above payment threshold in percentages where you disconnect from your peer (default 25)
# MOP_PAYMENT_TOLERANCE_PERCENT=25
## postage stamp contract address
# MOP_POSTAGE_STAMP_ADDRESS=
## ENS compatible API endpoint for a TLD and with contract address, can be repeated, format [tld:][contract-addr@]url
# MOP_RESOLVER_OPTIONS=[]
## enable swap (default true)
# MOP_SWAP_ENABLE=true
## swap ethereum blockchain endpoint (default ws://localhost:8546)
# MOP_SWAP_ENDPOINT=ws://localhost:8546
## swap factory address
# MOP_SWAP_FACTORY_ADDRESS=
## legacy swap factory addresses
# MOP_SWAP_LEGACY_FACTORY_ADDRESSES=
## initial deposit if deploying a new chequebook (default 10000000000000000)
# MOP_SWAP_INITIAL_DEPOSIT=10000000000000000
## gas price in wei to use for deployment and funding (default "")
# MOP_SWAP_DEPLOYMENT_GAS_PRICE=
## enable tracing
# MOP_TRACING_ENABLE=false
## endpoint to send tracing data (default 127.0.0.1:6831)
# MOP_TRACING_ENDPOINT=127.0.0.1:6831
## service name identifier for tracing (default mop)
# MOP_TRACING_SERVICE_NAME=mop
## proof-of-identity transaction hash
# MOP_TRANSACTION=
## log verbosity level 0=silent, 1=error, 2=warn, 3=info, 4=debug, 5=trace (default info)
# MOP_VERBOSITY=info
## send a welcome message string during handshakes
# MOP_WELCOME_MESSAGE=
## triggers connection to main network
# MOP_MAINNET=
